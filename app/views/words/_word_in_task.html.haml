%div
  - if word.paradigm_id
    = link_to word.text, new_paradigm_path(:word_id => word)
    [in
    != links_to_paradigm_edit(word.homonyms)
    ]
  - else
    = link_to word.text, new_paradigm_path(:word_id => word)
